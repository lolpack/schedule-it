#include <iostream>
#include <string>
#include <fstream>

using namespace std;

int main()
{
  string jobs;
  ifstream file("jobs.txt");

  if(file.is_open())
	while(file.good())
	  {
		getline(file, txt);
		cout << txt << endl;
	  }

  return ;
}
